// Import standard input/output library in form of header
#include <stdio.h>

// Every code run at function named main. Function main return integer type
int main() 
{
	// printf is print for C, just like print in Python and console.log in JS
	printf("This will print: Hello, World!"); // Always end with semi-colon

	return (0); // This is what this function return. In this case, 0 means no issue.
}